{% extends "base.html" %}
{% block title %} Make Deck {% endblock %}
{% block body %}
<h1> Make Deck </h1>
    <form action = "/main">
      <input type="submit" value="Done">
    </form>
    <table class="table">
  <thead>
    <tr>
    <th scope="col">Name</th>
    <th scope="col">Attack(Hockey)</th>
    <th scope="col">Defense(Hockey)</th>
    <th scope="col">Type(Pokemon)</th>
    </tr>
  </thead>
  <tbody>
  {% for i in players %}
  <tr>

    <td>{{ i[0] }}</td>
    <td>{{ i[1] }}</td>
    <td>{{ i[2] if i[2] != "0" else "N/A" }}</td>
    <td>{{ i[3] }}</td>
  </tr>
  {% endfor %}
</tbody>
</table>
	<p>{{nhl}}</p>
	<p>{{pokemon}}</p>
	<p>{{pokemonType}}</p>

{% endblock %}
